import { Homepage } from "../components";
const Home = () => {
	return (
		<section className="flex-1 py-3 px-2 flex items-center justify-center">
			<Homepage />
		</section>
	);
};

export default Home;

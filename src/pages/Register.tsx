import { RegisterComponent } from "../components";

const Register = () => {
	return (
		<section className="flex-1 py-3 flex items-center justify-center px-2 flex-col">
			<RegisterComponent />
		</section>
	);
};

export default Register;
